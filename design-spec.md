# dulog 设计交付规格（Design Spec v1）

> 交付对象：开发实现（Tauri + Vite + React/TS 前端）。本文档为唯一设计依据，色值、尺寸、状态均以此为准。
> 设计稿参照：对话中已确认的两版高保真原型（主界面 / 新建连接与空白编辑迭代版）。

---

## 1. 设计令牌（Design Tokens）

### 1.1 颜色

| Token | 值 | 用途 |
|---|---|---|
| `--bg-editor` | `#1e1e1e` | 编辑器/日志视图底色 |
| `--bg-panel` | `#252526` | 左右侧栏底色 |
| `--bg-titlebar` | `#323233` | 顶部标题栏 |
| `--bg-tabbar` | `#2d2d2d` | 标签栏底色（非激活标签） |
| `--bg-toolbar` | `#282828` | 搜索工具栏底色 |
| `--bg-item-active` | `#37373d` | 列表项/文件树选中态 |
| `--bg-line-active` | `#2a2d2e` | 编辑器当前行高亮 |
| `--bg-line-match` | `#3a3320` | 搜索命中行整行底色 |
| `--accent` | `#4ea1f0` | 强调蓝：激活标签上边线、选中项左边线、链接文字、光标 |
| `--accent-btn` | `#0e639c` | 主按钮底色（新建 SSH 连接 / 导出结果） |
| `--statusbar` | `#007acc` | 底部状态栏底色 |
| `--hl-match` | `#e5a44b` | 关键词高亮底色（文字用 `#1e1e1e`，圆角 2px，左右 padding 2px） |
| `--text-primary` | `#d4d4d4` | 正文 |
| `--text-secondary` | `#bdbdbd` / `#9a9a9a` | 次级文字/未激活标签 |
| `--text-muted` | `#8a8a8a` | 分组标题（uppercase） |
| `--text-dim` | `#6a6a6a` / `#7a7a7a` | 行号、文件大小注记 |
| `--log-warn` | `#e5a44b` | WARN 行文字 |
| `--log-error` | `#f0d0a0` | ERROR 行文字 |
| `--log-fatal` | `#e06c6c` | FATAL 行文字；亦用于删除按钮 |
| `--dot-online` | `#28c840` | 连接在线状态点 |
| `--dot-offline` | `#6a6a6a` | 连接离线状态点 |
| 分隔线 | `#000`（0.5px）；控件描边 `#3a3a3a` | 面板分割 / 次级按钮边框 |

### 1.2 字体

| 场景 | 字体 | 字号/行高 |
|---|---|---|
| 日志/编辑器正文、搜索结果 | 等宽（`Consolas / SF Mono / JetBrains Mono, monospace`） | 12.5–13px / 1.7 |
| UI 文字（侧栏、菜单、标签） | 系统无衬线 | 12–13px |
| 分组标题 | 系统无衬线，uppercase，letter-spacing 0.08em | 11px |
| 状态栏 | 系统无衬线 | 11.5px |

### 1.3 圆角与间距

- 按钮/输入框圆角：6px；高亮 mark 圆角：2px
- 列表项纵向 padding：7px；文件树行 padding：4px；面板内边距：10–12px

---

## 2. 布局结构

```
┌ 标题栏 36px（macOS 红黄绿灯 / Windows 侧最小化关闭；菜单：文件 编辑 视图 连接 帮助）
├──────────────┬──────────────────────────┬──────────────┐
│ 左栏 220px    │ 中部编辑区 flex:1          │ 右栏 260px    │
│ · 连接区      │ · 标签栏                  │ · 搜索结果/大纲 │
│ · 文件树      │ · 搜索工具栏（日志标签时）   │              │
│              │ · 日志/文本视图（虚拟滚动）   │              │
├──────────────┴──────────────────────────┴──────────────┘
└ 状态栏 ~26px（#007acc）
```

左右栏可拖拽调宽（建议 min 160px / max 400px），可折叠。

---

## 3. 组件规格

### 3.1 左栏 · 连接区
- 顶部主按钮 **「+ 新建 SSH 连接」**：宽度撑满（左右 margin 10px），高约 30px，`--accent-btn` 底、白字 12.5px，圆角 6px。点击弹出连接表单（见 3.6）。
- 连接列表项：状态点(8px 圆) + 名称(单行省略) + hover 操作区。
  - 选中态：底色 `--bg-item-active` + 左侧 2px `--accent` 边线。
  - hover 显示 **编辑 ✎**（灰）与 **删除 ×**（`#e06c6c`）图标按钮 20×20。
  - 删除需二次确认气泡：「确定删除 {name}？ 删除 / 取消」，删除为红色按钮。
- 固定项「本地文件夹」：点击调起系统文件夹选择框（Tauri dialog）。

### 3.2 左栏 · 文件树
- 目录行：▸/▾ 折叠符，缩进 16px/级。
- 文件行：文件名 + 右侧灰色文件大小（如 `2.4 GB`）。选中态同上。
- 仅列出 `*.log` `*.txt` `*.out` 及无扩展名文本文件（可在设置中放开）。

### 3.3 中部 · 标签栏
- 标签高约 32px；激活标签：底色 `--bg-editor`、白字、上边 2px `--accent`；非激活：灰字。
- 每个标签带关闭 ×；未保存文档以 ● 替代 ×（hover 换回 ×）。
- 栏尾常驻 **「+ 新建空白文本」**（`--accent` 蓝字）：新建 `未命名-N` 标签，进入可编辑空白文档。

### 3.4 中部 · 搜索工具栏（日志/文本标签均显示）
- 输入框（等宽字体，聚焦时描边 `--accent`）+ 命中计数 `3 / 128` + 开关按钮组：`Aa` 大小写、`.*` 正则（建议补 `W` 全词）。26×26，激活态描边/文字变 `--accent`。
- 回车下一处、Shift+回车上一处；Esc 关闭。

### 3.5 中部 · 日志/文本视图
- 行号栏右对齐，宽 44–52px，色 `--text-dim`；当前行行号亮色 `#c8c8c8`。
- **虚拟滚动**：仅渲染可视行 ±缓冲，行高固定 22px（13px × 1.7）。
- 日志级别着色：WARN `--log-warn`；ERROR `--log-error`；FATAL `--log-fatal`；INFO 默认色。
- 搜索命中：行底 `--bg-line-match`，关键词 `<mark>` 样式 `--hl-match`。
- 空白文本编辑态：当前行底色 `--bg-line-active`，光标 1.5px `--accent` 竖线。
- 日志文件默认只读；空白文本/小文件可编辑（大文件编辑需提示转只读或分段加载）。

### 3.6 弹窗 · 新建/编辑 SSH 连接
- 居中模态 420px 宽，底 `--bg-panel`，圆角 8px。
- 字段：名称、主机、端口(默认22)、用户名、认证方式(密码/私钥单选)、密码或私钥路径、备注。
- 底部：取消（描边按钮）/ 保存并连接（`--accent-btn` 主按钮）。
- 连接中在按钮内显示 spinner；失败在表单顶部红条提示错误信息。

### 3.7 右栏 · 搜索结果 / 大纲
- 日志标签激活时：标题「搜索结果 · N 处」，每条 `L{行号} + 命中上下文`（关键词同款高亮），点击跳转并定位中部视图；当前条目选中态同列表规范。底部「导出结果」（主按钮）/「仅看匹配」（描边按钮）。
- 空白文本标签激活时：显示大纲/占位说明。

### 3.8 状态栏
- 左：`行 x, 列 y` · 编码(UTF-8) · 换行(LF) · 文档类型；右：`已索引 N 行` · 文件大小；未保存时显示「未保存的更改 ●」。

---

## 4. 关键交互与性能要求

1. **大文件秒开**：Rust 侧按字节偏移建立行索引（后台线程增量构建），前端通过 `read_lines(offset, count)` 命令按需取行；打开 GB 级文件首屏 < 500ms。
2. **搜索**：Rust 侧流式扫描（本地）或远程 `grep -n`（SSH），结果分页回传；前端只高亮可视区。
3. **SSH**：沿用项目已验证的 MSYS2 环境注入 + ASKPASS 密码方案（见 `.workbuddy/memory/2026-07-24.md`）；远程文件通过流式读取，不整文件下载。
4. **跨端**：macOS 红黄绿灯用系统原生（`titleBarStyle`），Windows 用自绘标题栏；快捷键 Cmd/Ctrl 对应映射。
5. 主题先做深色单主题，令牌全部走 CSS variables，为后续浅色主题留口。

---

## 5. 待补设计（下一轮）

- 删除连接的二次确认气泡视觉稿
- 新建 SSH 连接表单弹窗视觉稿
- 空状态 / 首次启动引导页
- 设置面板（文件类型过滤、字号、主题）
